# CS50-Python
All of my projects during the CS50x course.

Week 0 :
- Indoor : a program that prompts the user for input and then outputs that same input in lowercase.
- Playback : a program that prompts the user for input and then outputs that same input, replacing each space with ...
- Faces : a program that prompts the user for input and returns that same input with any :) converted to 🙂 and any :( converted to 🙁.
- Einstein : a program that prompts the user to type an inteher that will be the (in kilograms) and then outputs the equivalent number of Joules as an integer. Using Einstein's formula.
- Tip : a program that prompts the user for typing the cost of the food and then the amount to tip (in a percentage) in order to output the amount of the tip in currency. 

Week 1 :
- Deep : a program that prompts the user for the answer to the Great Question of Life, the Universe and Everything, outputting Yes if the user inputs 42 or (case-insensitively) forty-two or forty two.             Otherwise output No.
- Bank : a program that prompts the user for a greeting. If the greeting starts with “hello”, output $0. If the greeting starts with an “h” (but not “hello”), output $20. Otherwise, output $100. 
- Extensions : a program that prompts the user for the name of a file and then outputs that file’s media type.
- Interpreter : a program that prompts the user for an arithmetic expression and then calculates and outputs the result as a floating-point value formatted to one decimal place. Assuming that the user’s                  input will be formatted as x y z, with one space between x and y and one space between y and z.
- Meal : a program that prompts the user for a time and outputs whether it’s breakfast time, lunch time, or dinner time. If it’s not time for a meal, it doesn’t output anything at all. Also each meal’s            time range is inclusive. For instance, whether it’s 7:00, 7:01, 7:59, or 8:00, or anytime in between, it’s time for breakfast.
